{
  "name": "writable-filestream",
  "version": "0.0.0",
  "description": "simple replacement for fs.createWriteStream",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "stream-buffers": "^0.2.5"
  }
}
